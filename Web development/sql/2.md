Here is a **short, exam-ready Query Builder method** (CodeIgniter style) to fetch **all posts along with the user names** who created them.

---

### ✅ **Assumption**

* `users` table → `id`, `name`
* `posts` table → `id`, `title`, `user_id`
* `posts.user_id` is a **foreign key** referencing `users.id`

---

## 🔹 **CodeIgniter Query Builder Code**

```php
$this->db->select('posts.*, users.name');
$this->db->from('posts');
$this->db->join('users', 'users.id = posts.user_id');
$query = $this->db->get();
$result = $query->result();
```

---

## 🧠 **Explanation (2 lines – for exam)**

* `join()` is used to combine `posts` and `users` tables using foreign key.
* It retrieves **all posts** along with the **name of the user** who created each post.

---

### 🔁 Optional (Left Join – if some posts have no user)

```php
$this->db->select('posts.*, users.name');
$this->db->from('posts');
$this->db->join('users', 'users.id = posts.user_id', 'left');
$query = $this->db->get();
```

---

Rahul, ye answer **direct likhne layak hai paper me** ✔️
Agar chaho toh main:
👉 **SQL JOIN version** bhi de sakta hoon
👉 Ya **CodeIgniter4 syntax**


Bas bolo 👍
