Rahul, yeh **direct exam-ready answer** hai. Tum dono options me se **koi ek** likh sakte ho paper me.

---

# ✅ OPTION 1

### **Query Builder: Insert record into `product` table & count total products**

### 🔹 Assumptions:

`product` table fields → `id`, `name`, `price`, `category`

---

### ✍ Insert Record (CodeIgniter Query Builder)

```php
$data = array(
    'name' => 'Laptop',
    'price' => 55000,
    'category' => 'Electronics'
);

$this->db->insert('product', $data);
```

---

### ✍ Count Total Products

```php
$total = $this->db->count_all('product');
echo $total;
```

---

### 🧠 Short Explanation (for exam)

* `insert()` method is used to add a new record.
* `count_all()` returns total number of rows from table.

---

# ✅ OPTION 2

### **MySQL Queries based on given table**

| id | User Name | email                                     | Password |
| -- | --------- | ----------------------------------------- | -------- |
| 1  | John      | [John@gmail.com](mailto:John@gmail.com)   | Pass123  |
| 2  | Smith     | [Smith@gmail.com](mailto:Smith@gmail.com) | Pass456  |
| 3  | Alice     | [alice@gmail.com](mailto:alice@gmail.com) | Pass789  |
| 4  | Brown     | [brown@gmail.com](mailto:brown@gmail.com) | Pass321  |
| 5  | Clark     | [clark@gmail.com](mailto:clark@gmail.com) | Pass654  |

---

## 1️⃣ Create Table with Primary Key & Constraints

```sql
CREATE TABLE users (
    id INT PRIMARY KEY,
    username VARCHAR(50) NOT NULL,
    email VARCHAR(50) UNIQUE,
    password VARCHAR(20) NOT NULL
);
```

---

## 2️⃣ Update Email of user whose name is Smith

```sql
UPDATE users 
SET email = 'smith_new@gmail.com'
WHERE username = 'Smith';
```

---

## 3️⃣ Insert New Record

```sql
INSERT INTO users VALUES (6, 'David', 'david@gmail.com', 'Pass999');
```

---

## 4️⃣ Delete Record whose name is Clark

```sql
DELETE FROM users 
WHERE username = 'Clark';
```

---

## 5️⃣ Display Records in Ascending Order of Username

```sql
SELECT * FROM users
ORDER BY username ASC;
```

---

## 📝 Exam Tip (10 Marks Guarantee)

Har sub-question me:
✔ Proper **SQL syntax**
✔ `PRIMARY KEY`, `UNIQUE`, `ORDER BY` jaise keywords
✔ Clear structure

👉 Itna likhne par **full marks milte hain**.

---

Rahul, agar chaho toh main:
👉 **“CodeIgniter login / session ka short program”**
👉 Ya **“JOIN / foreign key wala SQL question”**

bhi turant bana dunga. 💪
